import * as _ from 'lodash';

// NOTE!!! Because usually people call lodash _ I think that it would be easier to call aigle wrapper arount it as __.
const __ = require('aigle');

__.mixin(_);

export default __;